# Отчет по лабораторной работе №5. Интеграция Frontend и Backend (XHR / Fetch)

**Цель** данной лабораторной работы — создание полноценного клиент-серверного приложения на основе разработанных ранее SPA-фронтенда (ЛР 3) и Express-бэкенда. В ходе работы мы отказываемся от захардкоженных данных (моков), подключаем клиент к реальному API, реализуем страницу добавления и редактирования услуг дата-центра, а также обходим ограничения политики CORS.

## Оглавление
- [1. Введение в клиент-серверное взаимодействие](#1-введение-в-клиент-серверное-взаимодействие)
- [2. Архитектура интеграции (FlowVector)](#2-архитектура-интеграции-flowvector)
- [3. Решение проблемы CORS](#3-решение-проблемы-cors)
- [4. Работа с XHR и Fetch API](#4-работа-с-xhr-и-fetch-api)
- [5. Создание страницы добавления и редактирования услуг](#5-создание-страницы-добавления-и-редактирования-услуг)
- [6. Реализация CRUD-операций на клиенте](#6-реализация-crud-операций-на-клиенте)
- [7. Дополнительные задания и модификации](#7-дополнительные-задания-и-модификации)
- [8. Вывод](#8-вывод)

---

## 1. Введение в клиент-серверное взаимодействие

Современные веб-приложения строятся по принципу разделения ответственности. **Фронтенд** (браузерная часть) отвечает только за отрисовку интерфейса и реакцию на действия пользователя. **Бэкенд** (серверная часть) отвечает за бизнес-логику, валидацию и работу с базой данных (в нашем случае — файл `stocks.json`).

Связь между ними происходит по протоколу HTTP с использованием архитектуры **REST API**. Фронтенд отправляет запросы (GET, POST, PATCH, DELETE) на определенные адреса (эндпоинты), а сервер возвращает ответ в формате `JSON`.

## 2. Архитектура интеграции (FlowVector)

В предыдущих работах класс `MainPage` использовал метод `getData()`, который возвращал статичный массив данных (Виртуальные серверы, Выделенные серверы и т.д.).
Теперь архитектура изменена:
1. Сервер Express.js запущен на порту `3000` (`http://localhost:3000/stocks`).
2. Клиентский код (запущенный, например, через Live Server на порту `5500`) запрашивает данные с сервера.
3. Фронтенд дожидается ответа сервера и только после этого рендерит компонент `CatCardComponent`.

## 3. Решение проблемы CORS

**CORS (Cross-Origin Resource Sharing)** — это механизм безопасности браузера, который по умолчанию запрещает веб-странице делать запросы к домену, порту или протоколу, отличному от того, с которого была загружена сама страница.
Поскольку наш клиент крутится на порту 5500, а API — на 3000, браузер заблокирует такие XHR/Fetch запросы.

Для обхода CORS в код бэкенда (`example-express/src/index.js`) был интегрирован специальный Middleware:
```javascript
// Настройка CORS для разрешения запросов с фронтенда
app.use((req, res, next) => {
    // Разрешаем запросы с любых источников (*)
    res.setHeader('Access-Control-Allow-Origin', '*');
    // Разрешаем нужные HTTP методы
    res.setHeader('Access-Control-Allow-Methods', 'GET, POST, PATCH, DELETE, OPTIONS');
    // Разрешаем передачу JSON заголовков
    res.setHeader('Access-Control-Allow-Headers', 'Content-Type');

    // Обработка предварительного preflight-запроса от браузера
    if (req.method === 'OPTIONS') return res.sendStatus(200);

    next();
});
```
Такой подход решает проблему на уровне сервера, инструктируя браузер пропустить запрос.

## 4. Работа с XHR и Fetch API

В JavaScript существует два основных нативных способа отправки сетевых запросов:

**1. XMLHttpRequest (XHR)** — старый способ, работающий на основе событий и коллбеков.
```javascript
const xhr = new XMLHttpRequest();
xhr.open('GET', 'http://localhost:3000/stocks');
xhr.onload = function() {
    if (xhr.status === 200) {
        const data = JSON.parse(xhr.responseText);
        // Отрисовка
    }
};
xhr.send();
```

**2. Fetch API** — современный стандарт, основанный на Промисах (Promises) и синтаксисе `async/await`. Он делает код асинхронных операций линейным и легко читаемым.

В нашем приложении был выбран `Fetch` для получения списка услуг в `MainPage`:
```javascript
async loadData() {
    try {
        const response = await fetch('http://localhost:3000/stocks');
        if (!response.ok) throw new Error('Ошибка сети');
        const data = await response.json();
        return data; // Возвращаем массив услуг дата-центра
    } catch (error) {
        console.error("Ошибка при загрузке данных:", error);
        return [];
    }
}
```

## 5. Создание страницы добавления и редактирования услуг

В рамках пятой лабораторной работы был реализован новый компонент-страница — `CreateProductPage` (для добавления/редактирования).
Страница содержит HTML-форму для ввода параметров услуги (Изображение, Название, Описание).

Разметка формы включает в себя поля `input` и `textarea`, стилизованные под темную тему корпоративного портала «FlowVector»:
```javascript
getHTML() {
    return `
        <div class="container text-light my-5">
            <h2 style="color: #ff6600;">Добавить новую услугу</h2>
            <form id="create-form">
                <div class="mb-3">
                    <label class="form-label">Ссылка на изображение</label>
                    <input type="text" id="input-src" class="form-control bg-dark text-light border-secondary" required>
                </div>
                <div class="mb-3">
                    <label class="form-label">Название услуги</label>
                    <input type="text" id="input-name" class="form-control bg-dark text-light border-secondary" required>
                </div>
                <div class="mb-3">
                    <label class="form-label">Описание</label>
                    <textarea id="input-desc" class="form-control bg-dark text-light border-secondary" rows="3" required></textarea>
                </div>
                <button type="submit" class="button">Сохранить</button>
                <button type="button" id="btn-cancel" class="button" style="background: transparent; border: 1px solid #ff6600; color: #ff6600;">Отмена</button>
            </form>
        </div>
    `;
}
```

## 6. Реализация CRUD-операций на клиенте

Чтобы форма не просто отрисовывалась, но и отправляла данные на сервер, используется перехват события `submit`.
Метод `e.preventDefault()` предотвращает стандартную перезагрузку страницы браузером.

### Create (POST-запрос)
После заполнения формы собирается JSON-объект и отправляется методом `POST`:
```javascript
async handleFormSubmit(e) {
    e.preventDefault(); // Отменяем перезагрузку страницы

    // Собираем данные из полей ввода
    const payload = {
        src: document.getElementById('input-src').value,
        name: document.getElementById('input-name').value,
        description: document.getElementById('input-desc').value
    };

    // Отправляем POST запрос к API
    await fetch('http://localhost:3000/stocks', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload)
    });

    // Возвращаемся на главную страницу после успешного сохранения
    const mainPage = new MainPage(this.parent);
    mainPage.render();
}
```

### Delete (DELETE-запрос)
В компоненте карточки (`CatCardComponent`) на кнопку «Удалить сразу» был навешен слушатель, который отправляет запрос на удаление карточки по её ID:
```javascript
async deleteItem(id) {
    await fetch(`http://localhost:3000/stocks/${id}`, {
        method: 'DELETE'
    });

    // Заново перерисовываем главную страницу, чтобы актуализировать список
    this.render();
}
```

## 7. Дополнительные задания и модификации

В рамках работы функционал был расширен для повышения удобства:

1. **Режим редактирования (Update / PATCH):** Класс `CreateProductPage` был спроектирован так, чтобы принимать опциональный параметр `editData`. Если в форму передаются готовые данные, она работает не в режиме создания (POST), а в режиме обновления. Форма автоматически предзаполняет поля `input.value = editData.name`, а при отправке делает `PATCH http://localhost:3000/stocks/${editData.id}`.
2. **Связка с кнопками на главной:** На главной странице добавлена кнопка "Добавить услугу", которая инициирует `new CreateProductPage(this.parent).render()`. Аналогично на каждой карточке услуги была добавлена кнопка "Редактировать", прокидывающая объект карточки в ту же форму.
3. **Обработка ошибок:** Если бэкенд выключен (порт 3000 недоступен), метод `loadData()` отлавливает ошибку через блок `catch` и показывает пользователю на экране красивое предупреждение компонентом `AlertComponent` ("Не удалось связаться с сервером").

## 8. Вывод

В ходе выполнения пятой лабораторной работы произошло полноценное слияние независимого клиентского приложения и серверного API. Я изучил концепции работы с `Fetch API`, научился отправлять асинхронные HTTP-запросы и обрабатывать ответы, минуя перезагрузку страниц (концепция SPA). Была успешно решена классическая проблема безопасности `CORS` на уровне Express-сервера, а также реализована динамическая форма создания и редактирования карточек с поддержкой методов `POST` и `PATCH`, полностью адаптированная под бизнес-логику проекта дата-центра «FlowVector».
